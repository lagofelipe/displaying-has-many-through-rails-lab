<h1>Appointments show Page</h1>

<%= link_to @appointment.doctor.name, doctor_path(@doctor) %><br>
<%= link_to @appointment.patient.name, patient_path(@patient) %><br>
<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>